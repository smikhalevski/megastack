{
  "name": "template",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "clean": "rimraf build",
    "build": "vite build && vite build --ssr",
    "preview": "[ -d build ]|| npm run build && vite preview",
    "preview-ssr": "[ -d build ]|| npm run build && cd build && node server.js",
    "typecheck": "tsc",
    "dev": "vite",
    "container": "[ -d build ]|| npm run build && docker rm --force $npm_package_name && docker rmi --force $npm_package_name && docker run --name $npm_package_name --interactive --tty --publish 3000:3000 $(docker build --tag $npm_package_name --quiet .)"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@types/html-minifier-terser": "^7.0.2",
    "autoprefixer": "^10.4.21",
    "html-minifier-terser": "^7.2.0",
    "lightningcss": "^1.30.1",
    "postcss-nested": "^7.0.2",
    "rimraf": "^6.0.1",
    "vite": "^7.1.3"
  },
  "dependencies": {
    "@hono/node-server": "^1.19.0",
    "doubter": "^5.2.0",
    "hono": "^4.9.5",
    "json-marshal": "^0.0.4",
    "react": "^19.1.1",
    "react-corsair": "^0.0.11",
    "react-dom": "^19.1.1",
    "react-executor": "^0.0.26",
    "react-hookers": "^6.3.0"
  }
}
