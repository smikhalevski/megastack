{
  "name": "example",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "clean": "rimraf build",
    "build": "vite build && vite build --ssr",
    "preview": "vite preview",
    "typecheck": "tsc",
    "dev": "vite",
    "ssr-preview": "cd build && node server.js"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "lightningcss": "^1.30.1",
    "postcss-nested": "^7.0.2",
    "vite": "^7.0.6"
  }
}
